import 'package:flutter/foundation.dart';

// TODO: providers/admin_provider.dart
// Tanggung jawab: Mengelola state dashboard admin (stats, top products, manage orders, status transitions, loading states).
class AdminProvider with ChangeNotifier {
  // State: stats, topProducts, orders, filterStatus, isLoading, errorMessage
}

import 'package:flutter/foundation.dart';

// TODO: providers/auth_provider.dart
// Tanggung jawab: Mengelola state autentikasi (login, register, auto-login, logout, profil, checking role).
class AuthProvider with ChangeNotifier {
  // State: user, token, isLoading, errorMessage
}

import 'package:flutter/foundation.dart';

// TODO: providers/cart_provider.dart
// Tanggung jawab: Mengelola state keranjang belanja (grand total, total items, loading states, update quantity lokal).
class CartProvider with ChangeNotifier {
  // State: cart, isLoading, errorMessage
}

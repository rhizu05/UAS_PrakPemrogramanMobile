import 'package:flutter/foundation.dart';

// TODO: providers/product_provider.dart
// Tanggung jawab: Mengelola state katalog produk (search, filter, sort, pagination, detail, loading/error states).
class ProductProvider with ChangeNotifier {
  // State: products, detailProduct, hasMore, page, isLoading, errorMessage
}

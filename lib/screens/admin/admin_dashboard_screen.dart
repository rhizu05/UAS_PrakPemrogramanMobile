import 'package:flutter/material.dart';

// TODO: screens/admin/admin_dashboard_screen.dart
// Tanggung jawab: Menampilkan 5 stats card (total produk, pesanan, pendapatan, pelanggan, pesanan pending)
// dalam layout grid 2 kolom.
class AdminDashboardScreen extends StatelessWidget {
  const AdminDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Admin Dashboard Screen'),
      ),
    );
  }
}

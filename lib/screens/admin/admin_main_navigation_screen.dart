import 'package:flutter/material.dart';

// TODO: screens/admin/admin_main_navigation_screen.dart
// Tanggung jawab: Menyediakan bottom navigation untuk admin (Dashboard, Top Products, Manage Orders, Profile).
class AdminMainNavigationScreen extends StatelessWidget {
  const AdminMainNavigationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Admin Main Navigation'),
      ),
    );
  }
}

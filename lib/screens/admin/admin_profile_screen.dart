import 'package:flutter/material.dart';

// TODO: screens/admin/admin_profile_screen.dart
// Tanggung jawab: Menampilkan profile dashboard khusus admin (avatar, nama, email, role admin)
// dan tombol logout yang memicu dialog konfirmasi.
class AdminProfileScreen extends StatelessWidget {
  const AdminProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Admin Profile Screen'),
      ),
    );
  }
}

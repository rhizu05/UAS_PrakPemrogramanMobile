import 'package:flutter/material.dart';

// TODO: screens/admin/top_products_screen.dart
// Tanggung jawab: Menampilkan grafik batang (Bar Chart) menggunakan fl_chart untuk produk terlaris
// dan list ranking produk terlaris di bawahnya.
class TopProductsScreen extends StatelessWidget {
  const TopProductsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Top Products Screen'),
      ),
    );
  }
}

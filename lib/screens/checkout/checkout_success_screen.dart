import 'package:flutter/material.dart';

// TODO: screens/checkout/checkout_success_screen.dart
// Tanggung jawab: Memberikan feedback sukses kepada customer setelah pesanan berhasil dibuat.
// Menyediakan navigasi menuju riwayat pesanan (Orders) dan halaman utama (Home).
class CheckoutSuccessScreen extends StatelessWidget {
  const CheckoutSuccessScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Checkout Success Screen'),
      ),
    );
  }
}

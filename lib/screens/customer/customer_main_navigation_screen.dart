import 'package:flutter/material.dart';

// TODO: screens/customer/customer_main_navigation_screen.dart
// Tanggung jawab: Menyediakan bottom navigation untuk customer (Home, Cart, Orders, Profile).
class CustomerMainNavigationScreen extends StatelessWidget {
  const CustomerMainNavigationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Customer Main Navigation'),
      ),
    );
  }
}

import 'package:flutter/material.dart';

// TODO: screens/order/order_history_screen.dart
// Tanggung jawab: Menampilkan riwayat pesanan customer dengan pagination, format ORD-8chars,
// status badge, dan empty/error/loading states.
class OrderHistoryScreen extends StatelessWidget {
  const OrderHistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Order History Screen'),
      ),
    );
  }
}

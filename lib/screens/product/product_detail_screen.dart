import 'package:flutter/material.dart';

// TODO: screens/product/product_detail_screen.dart
// Tanggung jawab: Menampilkan detail lengkap produk (rating rata-rata, review count, stok, list reviews)
// serta formulir tambah ulasan dan tombol tambah ke keranjang belanja.
class ProductDetailScreen extends StatelessWidget {
  const ProductDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Product Detail Screen'),
      ),
    );
  }
}

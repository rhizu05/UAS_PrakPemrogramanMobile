import 'package:flutter/material.dart';

// TODO: screens/profile/customer_profile_screen.dart
// Tanggung jawab: Menyediakan update profile customer (nama lengkap & nomor telepon)
// dan tombol logout dengan popup dialog konfirmasi.
class CustomerProfileScreen extends StatelessWidget {
  const CustomerProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Customer Profile Screen'),
      ),
    );
  }
}
